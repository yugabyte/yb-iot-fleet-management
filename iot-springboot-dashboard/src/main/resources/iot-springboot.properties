#Cassandra  properties
cassandra.host=${CASSANDRA_HOST:localhost}
cassandra.port=${CASSANDRA_PORT:9042}
cassandra.keyspace=traffickeyspace
cassandra.username=${CASSANDRA_USERNAME:}
cassandra.password=${CASSANDRA_PASSWORD:}
cassandra.sslEnabled=${CASSANDRA_SSL_ENABLED:false}
cassandra.certificate=${CASSANDRA_SSL_CERTIFICATE:}